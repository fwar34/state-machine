{
  "configurations": [
    {
      // "name": "Linux",  // 或 "Win32"、"Mac"，根据系统选择
      "name": "Win32",  // 或 "Win32"、"Mac"，根据系统选择
      // "compileCommands": "${workspaceFolder}/build/compile_commands.json",  // 指定文件路径
      "compileCommands": "${workspaceFolder}/compile_commands.json",  // 指定文件路径
      "intelliSenseMode": "gcc-x64",  // 匹配编译器（如 gcc、clang、msvc）
      "cppStandard": "c++20"  // 匹配项目使用的 C++ 标准
    }
  ],
  "version": 4
}